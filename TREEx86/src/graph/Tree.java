package graph;

/**
 *
 * @author Kenneth
 */
public class Tree {
    private DataTree dt;
    public Tree(int numOfLayers){
        dt = new DataTree(new DataTree.RootNode());
        for(int i = 0; i < numOfLayers; i++){
            
        }
    }
}
